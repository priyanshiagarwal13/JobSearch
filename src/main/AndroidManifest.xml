<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.jobsearch">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.JobSearch"
        tools:ignore="ExtraText">

        <!-- Your activities here -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MainActivity2"></activity>
        <activity android:name=".PersonalPreferences"
            tools:ignore="Instantiatable"></activity>
        <activity android:name=".SignUpEmployee" />
        <activity android:name=".ChooseCompanies" />
        <activity android:name=".CompanyDetails2" />
        <activity android:name=".UniversityDetails"
            tools:ignore="Instantiatable" /></application>


</manifest>